<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:viewModels="clr-namespace:XamarinApp;assembly=XamarinApp"
             x:Class="XamarinApp.NotifyPage"
             Title="Notiz">
  <ContentPage.BindingContext>
    <viewModels:NotifyModel/>
  </ContentPage.BindingContext>  
    <ContentPage.Content>
    <ListView ItemsSource="{Binding Notify}" IsGroupingEnabled="True" GroupDisplayBinding="{Binding Title}" HasUnevenRows="True">
      <ListView.ItemTemplate>
        <DataTemplate>
          <SwitchCell Text="{Binding Text}" On="{Binding On}" />
        </DataTemplate>
      </ListView.ItemTemplate>
      <ListView.GroupHeaderTemplate>
          <DataTemplate>
            <TextCell Text="{Binding Title}" />
          </DataTemplate>
        </ListView.GroupHeaderTemplate>
      </ListView>          
    </ContentPage.Content>
</ContentPage>